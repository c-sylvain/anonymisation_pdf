[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "anonymisation_pdf"
version = "0.1.0"
description = "PDF anonymisation tools"
requires-python = ">=3.10"
